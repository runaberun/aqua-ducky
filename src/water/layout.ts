// Shared sizing for the Today hero (kept in one place so the geometry in
// useWaterApp's ring math and the TodayA markup never drift apart).
export const RING = { box: 288, center: 144, r: 130, stroke: 9 }
export const GLASS = { w: 128, h: 180 }
